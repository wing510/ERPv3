## 每日變更紀錄

> 固定提醒（請遵循）：
> - 每次工作日結束都要更新本檔，新增當天日期區塊（`## YYYY-MM-DD`）。
> - 每天用同一套條列邏輯：由 1. 開始連號、一點一行，文字以「做了什麼＋目的/效果」為主。
> - 每天約10點每點一句為主、白話、不含欄位英文名
> - 只寫當天新增/調整的重點（避免重複前一天已記錄的內容）。
> - 日期區塊請維持新到舊排序（最新日期在最上面）。
> - 若今天已存在同日期區塊，請在該區塊續寫新增項目（不要再新增重複日期標題）。

## 2026-04-28
1. 銷售單主檔版面重排：銷售/下單日互換位置，備註改跨多欄並貼近日期，降低視線來回與漏填機率。
2. 出貨主檔新增「出貨人員」下拉：放在出貨日期旁邊並設為必填，過帳時一併寫入，建立責任歸屬方便追查。
3. 出貨單列表資訊補強：新增「出貨」欄位顯示出貨人員，且欄位命名更口語（銷售/出貨），方便現場快速掃描。
4. 出貨單列表顯示一致化：銷售與出貨都優先顯示人名，找不到人名才回退顯示 ID，避免只看到代碼難讀。
5. 出貨單列表欄位順序調整：狀態與出貨日期位置互換，讓日期更靠前更符合查詢習慣。
6. 出貨人員資料結構補齊：前後端與工作表欄位對齊，避免新增欄位後寫入錯位或漏寫造成追溯斷點。

## 2026-04-27
1. 收貨入庫流程更清楚：取消上方「整張倉別」，改為每列明細選倉別並推導唯一倉別送出，避免誤以為只能單倉且不知哪列屬哪倉。
2. 收貨入庫來源下拉優化：新增「顯示已收完（CLOSED）」勾選；預設只列可收的單（OPEN/PARTIAL），需要時再切回顯示結案單。
3. 收貨入庫來源篩選更穩：狀態字串正規化（避免舊資料寫成「CLOSED（…）」導致判斷失準），並確保未勾選時不混入結案 PO。
4. Import 明細欄名對齊：明細表頭從「收貨進度」改回「狀態」，與採購明細一致。
5. Import 建立後草稿消失：報單建立/更新成功後自動重載該張報單，讓草稿列立即消失並以已存檔資料為準。
6. Import 狀態與心智模型對齊：提示/註解統一 OPEN/PARTIAL/CLOSED/CANCELLED，避免使用者看到 PARTIAL 造成誤會。
7. DEV 端點更新：前端 DEV 後端網址更新為最新部署，避免指到舊 exec 造成測試與驗收混亂。
8. 後端報單狀態回寫一致化：進口報單狀態改依申報量與已收量推導 OPEN/PARTIAL/CLOSED（排除已作廢收貨），收貨過帳與作廢都用同一套規則回寫。
9. Lots / Movements 清單一致化：Lots 合併 Lot ID/產品欄、移除空值類型欄、按鈕樣式與高度對齊；Movements 也改成同樣的 Lot ID/產品欄呈現並修正載入/分組對齊。
10. 追溯與一致性補強：Movements「異動類型」改人話分類、藍色驚嘆號補齊轉倉 QA 規則；CLOSED 中文改成「已收完」；自動產生 ID 的規則與 placeholder 全站對齊。

## 2026-04-26
1. 追溯體驗整理：交易鏈查詢可用單號直接查（不必懂 TX），並補齊向下追的篩選刷新與狀態中文一致化。
2. 出貨列表防誤觸：列表移除作廢入口，需載入後再作廢，降低誤操作風險。
3. 採購/進口狀態用語對齊：列表狀態與明細顯示統一口徑（未收/部分收貨/已收完），降低跨模組理解成本。
4. 收斂重複（持續）：多個模組把重複的清空/重設、欄位寫入與提示邏輯收斂成可重用的小工具，讓後續改規則不用逐頁重複改。

## 2026-04-25
1. 交易頁 UX 補強：採購/銷售/收貨/出貨/加工在關鍵欄位切換時，會提示並清空不相容輸入或草稿，避免殘留造成誤送。
2. 必填提示統一：多個高頻交易頁把零散必填錯誤收斂成一條「缺少必填：…」，降低漏填與試錯成本。
3. Lots 防誤操作：切換篩選條件時若 QA/日期視窗仍開啟，會先提示並關閉視窗後再刷新，避免對錯批次操作。
4. Trace 防殘留：Lot/transaction 輸入變更會先清空舊查詢結果，避免殘留內容造成誤判。
5. 庫存異動/拆批/合批防殘留：切換來源 Lot 時自動清空數量/原因等欄位，且必要時跳確認以避免把上一筆帶到下一筆。
6. Import 防誤存：報單切換或重設時，若已有品項草稿會先確認並清空，避免草稿誤存到另一張報單。
7. 切換模組更乾淨：全站切模組時會先關閉可能殘留的遮罩/視窗，避免遮住新畫面造成「以為卡住」。
8. Sheet 快取隔離：DEV/PROD 的 Sheet 環境資訊快取依後端網址分開，避免同機切環境時快取互相污染。

## 2026-04-24
0. ERP_DB資料分成兩套：DEV（桌機測試）/ PROD（GitHub 正式）。各自使用不同的 Google 試算表與 Apps Script 部署網址，避免測試資料影響正式資料
1. Apps Script設定改為同時維護 DEV/PROD 兩個後端網址，並可依網址自動選用或用 `?env=DEV|PROD` 強制切換，減少每次上線手動改錯的風險。
2. Sheet 按鈕改成依後端回傳的環境試算表開啟（不再正式開到測試），並保留各按鈕可直接開到對應分頁。
3. Sheet 開啟速度優化：第一次取得環境資訊後會快取（含重整後仍可快速開啟），減少每次點 Sheet 都要等待的延遲。
4. 補齊加工投料/產出兩張工作表的分頁對照（process_order_input/output），並同步更新對照文件避免缺漏。
5. 新增回歸測試清單文件（每次改版照表勾選），把部署/快取、登入權限、主檔、收貨→批次→出貨→追溯、作廢/取消、Sheet、Logs 的最短驗證路徑寫清楚。
6. 主檔資料品質一致化：產品/供應商/客戶/倉庫/使用者的必填口徑、更新防呆、停用前確認、狀態權限（僅主管可改）做統一，降低誤停用與更新成空值的風險。
7. 主檔必填再加嚴：產品單位必填；供應商類型/可用流程必填；客戶分類必填；倉庫類別必填，並同步表單 `*` 與藍色驚嘆號說明。
8. 主檔列表功能補強：客戶列表新增「分類」下拉篩選；倉庫列表新增「類別」欄位與「類別」下拉篩選，方便快速縮小範圍。
9. 主檔列表的藍色驚嘆號內容全部統一成固定格式（流程/規則/常見提示），並讓 5 個主檔的關鍵字搜尋都包含「備註」且文件與實際搜尋行為一致。
10. 主檔 UI 細節對齊：關鍵字輸入框寬度統一；備註欄位改成跨兩欄（含倉庫地址/備註），閱讀與填寫更順；產品關鍵字搜尋移除多單位換算設定，避免搜尋雜訊。

## 2026-04-23
1. 主檔「狀態（使用中/停用）」改成非主管角色無法修改：下拉直接反灰並提示原因，避免助理誤停用關鍵資料。
2. 後端同步加一道保護：主檔狀態被改動時會再檢查操作者角色，不符合就直接拒絕，避免繞過畫面直接呼叫。
3. 儀表板初始化改成「有權限才載入對應資料」，避免沒開權限的帳號一進來就跳權限不足。
4. 追溯模組快取載入改成「能讀就讀、不能讀就跳過」，避免部分資料無權限導致整個畫面卡住。
5. 倉庫主檔的建立/更新補上更完整的錯誤承接，避免失敗時瀏覽器 Console 出現未捕捉錯誤造成誤會。
6. 停用前的「是否被使用」背景檢查改為安靜模式：不再因為相關資料無讀取權限就跳一堆錯誤干擾操作。
7. 針對線上快取問題，新增多支關鍵腳本的版本參數，確保上線後使用者能確實載入新版修正。
8. 產品/客戶腳本也補上版本參數，修正線上仍吃到舊碼導致「一直跳權限不足」的情況。
9. 更新後端端點設定，前端可快速切換到新部署網址，降低換網址造成全站失效的風險。
10. 文件補齊：系統風險盤點改成主管可讀的「風險＋改善方向」，避免只剩工程術語難以溝通。

## 2026-04-22
1. 登入體驗補強：登入遮罩底部加入「登入中，請稍等…」提示，避免等待幾秒被誤認為當機。
2. 右上角顯示改為「角色中文 - 帳號」格式，讓現場操作一眼能確認目前身分。
3. 登入成功提示改成「歡迎登入（角色-帳號 姓名）」更口語，降低新手不確定是否已登入的焦慮。
4. Google 登入錯誤提示更清楚（例如憑證不符/設定錯誤），減少只看到失敗卻不知道怎麼處理。
5. Google 登入流程調整：避免帶入舊登入資訊干擾，降低「看似登入成功但後續一直說未登入」的機率。
6. 當系統偵測到登入狀態過期時，會引導重新登入並清掉舊狀態，避免卡在半登入狀態。
7. 權限不足的錯誤提示統一用語，讓使用者更容易判斷是「沒開權限」而不是系統壞掉。
8. 出現權限不足時的處理改得更穩定：不讓模組初始化因為單一資料讀取失敗就整個中斷。
9. 常用腳本加入版本參數，降低 GitHub Pages 快取導致修正延遲生效的狀況。
10. 新增一頁「最近增修」說明稿，整理近期變更與回歸檢查項目，方便對內交代。

## 2026-04-19
1. 出貨、銷售、採購、進口、收貨、加工、批次等流程頁面同步更新，讓多個模組的操作與顯示更一致。
2. 追溯頁面同步調整，讓查詢流程與提示更貼近實際使用，降低「看起來像壞掉」的誤判。
3. 這天的改動屬於「多模組一起對齊」類型，目標是減少不同頁面規則不一造成的困惑。

## 2026-04-18
1. 庫存異動頁面調整：畫面與操作流程再對齊，讓填寫與查詢更順，減少誤操作。

## 2026-04-17
1. 多個主檔與流程頁面同步更新（產品/客戶/供應商/倉庫/收貨/批次/進口/加工等），讓版型與互動更一致。
2. 拆批/合批相關程式更新，讓拆併流程的操作更穩定、可追溯。
3. 更新整包版本檔案，方便備份與回溯不同版本。

## 2026-04-14
1. 更新品牌 Logo 圖檔，讓登入/介面視覺更一致。
2. 多個頁面模板同步整理（拆批/合批/採購/操作紀錄/倉庫庫存等），讓版型一致、維護更容易。
3. 更新整包版本檔案，方便備份與回溯不同版本。

## 2026-04-11
1. 收貨明細九欄改固定比例，項次收窄，數量／已收／剩餘不再被撐得過寬；寬螢幕維持不出現橫向捲軸。
2. 收貨在小螢幕改給表格足夠寬並允許橫向滑動，加一句提示，避免日期與本次收貨欄被裁切。
3. 收貨表頭與相關提示改為「本次收貨」，數量欄比例與輸入寬度依實際操作再收斂。
4. 批次列表套用欄寬與窄螢幕橫滑邏輯；表頭改「總數」「可用」；操作鈕改兩行小字並拉近，欄寬依回饋微調。
5. 產品多單位換算改矩陣編排：一句話內選投料單位，每列填產出量與回收單位；箭頭只放在列前，拿掉句尾重複符號。
6. 多單位區塊移除長段說明與輸入框內「每 1 投料」類占位；空白下拉改顯示「回收單位」；數量與單位兩框略縮小。
7. 小螢幕表單改單欄直排（含庫存異動編輯區），手機填主檔與單據較順。
8. 使用者列表「角色」改以中文顯示，搜尋也可打中文角色；代碼放在提示裡供對照。
9. 示範用多單位介面頁與實際產品頁採用版型說明對齊，方便日後比對其他設計方案。
10. 批次列表在小螢幕補一行「可橫滑」提示，與收貨一致，減少找不到右側欄位的狀況。

## 2026-04-10
1. 銷售單兩個更新按鈕補上不能按時的原因提示，避免使用者不知道卡在哪。
2. 出貨「選擇 Lot」按鈕補上不能按時的原因提示，並說明自動分配模式不需手選。
3. 針對「不能按」的按鈕提示做一致化整理，降低各模組規則不一致造成的困惑。
4. 效能警示加入觀察管道，能更快看出哪些列表載入偏慢、方便後續優化。
5. 協助確認重新部署後的後端網址可正常回應，避免前端指到舊網址造成全站失效。
6. 前端設定檔更新後端網址，減少每次重新部署需要逐頁改連結的風險。
7. 作廢/流程用語持續對齊，讓使用者看到的提示更接近實際規則與限制。
8. 針對全站排查建立可追蹤方式，遇到卡住能定位到是哪個功能在報錯或超時。
9. 強化錯誤資訊的可讀性與一致性，讓第一時間能用提示判斷是資料規則還是連線問題。
10. 整體按鈕互動規則更貼近「不能用就反灰、且要說原因」的 UX 目標。

## 2026-04-09
1. 將系統自動寫入的追溯/作廢註記與人工備註分開存放，避免作廢後被手動修改覆蓋。
2. 採購、進口、銷售、出貨、加工等單據加入「系統註記」欄位，後續追溯更清楚。
3. 後端更新單據結構與寫入規則，避免一般更新把系統註記改掉，保護稽核資訊。
4. 加工回收作廢與整單取消改把作廢原因寫到系統註記，人工備註保留給使用者。
5. 新增採購單/進口報單/銷售單的作廢流程入口，統一由後端寫入作廢原因與記錄。
6. 前端作廢操作改走後端流程，避免每個畫面各寫一套導致格式不一致。
7. 同步補齊前後端欄位一致性，降低新增欄位後寫入錯位造成資料混亂的風險。
8. 重新部署後更新前端連線設定，確保全站功能能連到最新後端。
9. 針對未設定或錯誤網址的情境提供更清楚的提示，減少「看起來一直在儲存」的誤會。
10. 整體追溯方向確立：系統寫的就放系統註記，人工備註保持乾淨可讀。

## 2026-04-03
1. 後台統一「列表載入中」寫法，讀資料前先顯示提示，避免空白誤判。
2. 產品、供應商、客戶、倉庫、使用者等主檔列表已套用載入提示。
3. 採購單、進口報單、銷售、出貨、加工單等單據列表已套用載入提示。
4. 批次管理、庫存異動、操作紀錄、倉庫庫存等畫面已套用載入提示。
5. 採購／銷售／出貨／併批／拆批／委外等明細表頭「#」改「項次」。
6. 收貨明細表：窄螢幕首欄可換行、列垂直對齊；選 PO／報單與切來源時顯示載入中，失敗有提示。
7. 採購收貨明細「項次」畫面改 1、2、3…，後端過帳鍵不變。
8. 已收列表併入收貨明細同一卡片、點開才載入，標題旁灰字說明作廢條件，移除重新載入鈕。
9. 作廢按鈕改直接帶收貨單號，修正誤判「尚未選單據」。
10. 作廢前視窗選原因（下拉為主、其他須填說明）並寫入備註；已收列表預判能否作廢，不可則反灰「無法作廢」、hover 可看原因。

## 2026-04-02
1. 出貨可依效期自動配 Lot（先到期先出），明細可自動拆行；可關閉，預設開，減少選錯批號。
2. 出貨填寫區重排：品項／Lot 與數量分區，系統帶出的量不開放誤改。
3. 出貨挑 Lot／列表改強調效期；產品文字統一「名稱（規格）」，不再混進編號。
4. 銷售列表：「載入」改 Edit，並加「出貨」捷徑帶單過去。
5. 全站產品下拉與列表顯示規則對齊（名稱＋規格為主）。
6. 庫存異動選 Lot：單行顯示批號、產品、倉別、QA、可用量；QA 狀態用語統一。
7. 庫存異動新增轉倉：指定量與目標倉，自動沖帳並產生新批號；支援一鍵轉該批全部可用量。
8. 轉倉與 QA 銜接：整批轉可走待 QA；只轉一部分須已放行；來源與新批 QA 狀態會同步，避免卡單。
9. 批次管理 QA：儲存中提示時機修正；Log／追溯按鈕字變短。
10. 倉別溫層全站統一；轉倉後庫存量會補正；讀列表減少重複與舊快取；銷售／出貨／收貨／委外／批次標題加流程與狀態提示。

## 2026-04-01
1. 新增倉庫主檔：可建／改／停用，溫層（常溫／冷藏／冷凍）、地址等欄位齊全，倉庫編號由系統產生。
2. 收貨、出貨、加工、拆併、手動庫存異動等流程倉別改連主檔；收貨必選倉，下拉顯示「名稱＋溫層」，避免手打錯倉。
3. 單據與批號：系統自動產生的追溯說明與人員備註分開存放，查帳不混在同一欄。
4. 新增「倉庫庫存」畫面：依倉別看產品彙總與批號明細，標示即將到期與已過期，方便先進先出。
5. 後端部署入口與路由補強，降低上線環境差異導致功能不可用。
6. 出貨／委外挑批號時一併顯示倉別，分倉時較不易拿錯倉。
7. 收貨畫面：來源類型預設「請選擇」、精簡雜訊；標題旁加收庫狀態提示。
8. 採購單號、進口報單號改唯讀並由系統自動產生，避免手填格式不一。
9. 批次管理標題加狀態提示（含異動讀取失敗提醒）；QA 放行／退回補「儲存中」提示。
10. 批次與庫存異動列表新增「倉別」欄、格式一致；庫存異動首版「轉倉」：移量到新倉並產生新批號，仍保留原採購／進口追溯。

## 2026-03-31
1. 銷售單新增「業務」欄位（可指定業務員、列表顯示、可搜尋），建立責任歸屬與業績分析基礎。
2. 主檔 ID 自動產生（產品/供應商/客戶），減少人工輸入與錯誤、ID 縮短更好讀。
3. 列表/下拉顯示優化：供應商/客戶改以「名稱」為主（不再顯示 ID-名稱），提升閱讀效率。
4. 各模組「儲存中」提示統一：提示顯示在實際點擊的按鈕旁，避免跑到別區塊造成誤解。
5. 名詞/介面一致化：SHIPPED 翻譯調整為「全數出貨」、左側選單文字縮短、品牌名稱改為「DINGLI 鼎力」、側邊欄寬度調整為 200px。
6. 收貨/採購/進口等流程的「已作廢資料」不再被計入彙總或判斷（避免作廢後仍被當作已收/已存在而阻擋操作）。
7. Movements（手動庫存異動）功能強化：新增用途（內部領用/樣品/報廢/其他）與給誰（領用/交付），並改善列表排序更直覺。
8. 庫存追溯字串整理方向確立：系統自動追溯字串與使用者備註分離（避免混在同一欄位造成稽核困難）。



